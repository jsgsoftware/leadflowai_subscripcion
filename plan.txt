1: Necesito que se conecte a una bd ya creada en postgresql esta host: 65.21.110.250  , port : 4503 , user: postgres , password : 63166d2cc665da686f10 , database name: leadflowai
2: necesito crear una tabla nueva con los siguientes campos:
CREATE TABLE leadflow_account_subscriptions (
  id SERIAL PRIMARY KEY,
  account_id INTEGER NOT NULL,
  plan VARCHAR(50) NOT NULL,          -- free, starter, pro, etc
  status VARCHAR(20) NOT NULL,         -- active, expired, suspended
  started_at TIMESTAMP NOT NULL,
  expires_at TIMESTAMP NOT NULL,
  trial BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),

  CONSTRAINT fk_account
    FOREIGN KEY (account_id)
    REFERENCES accounts(id)
    ON DELETE CASCADE
);
